import { SortTableDataPipe } from './sort-table-data.pipe';

describe('SortTableDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTableDataPipe();
    expect(pipe).toBeTruthy();
  });
});
